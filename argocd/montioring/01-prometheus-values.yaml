## ============================================================
## 01-prometheus-values.yaml
## Chart: prometheus-community/kube-prometheus-stack
## Version: 67.4.0
## ============================================================

nameOverride: ""
fullnameOverride: ""

## ── Grafana (disabled - we deploy it separately) ─────────────
grafana:
  enabled: false

## ── Prometheus ───────────────────────────────────────────────
prometheus:
  enabled: true

  prometheusSpec:
    ## Retention
    retention: 15d
    retentionSize: "10GB"

    ## Enable remote-write receiver so Alloy can push metrics
    enableRemoteWriteReceiver: true

    ## Watch all ServiceMonitors/PodMonitors cluster-wide
    ## (namespace filtering handled by namespaceSelector below)
    serviceMonitorSelector: {}
    serviceMonitorNamespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values:
            - monitoring
            - retail-store-dev

    podMonitorSelector: {}
    podMonitorNamespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: In
          values:
            - monitoring
            - retail-store-dev

    ruleSelector: {}
    ruleNamespaceSelector: {}

    ## Storage using your ebs-sc StorageClass
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: ebs-sc
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 20Gi

    ## Resources
    resources:
      requests:
        cpu: 500m
        memory: 1Gi
      limits:
        cpu: 1000m
        memory: 2Gi

    ## Security context
    securityContext:
      runAsNonRoot: true
      runAsUser: 65534
      fsGroup: 65534

    ## Tolerations to run on any node
    tolerations:
      - operator: Exists
        effect: NoSchedule
      - operator: Exists
        effect: NoExecute

  service:
    type: ClusterIP
    port: 9090

## ── Alertmanager ─────────────────────────────────────────────
alertmanager:
  enabled: true

  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: ebs-sc
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 2Gi

    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 200m
        memory: 256Mi

    securityContext:
      runAsNonRoot: true
      runAsUser: 65534
      fsGroup: 65534

## ── Node Exporter ────────────────────────────────────────────
nodeExporter:
  enabled: true

prometheus-node-exporter:
  tolerations:
    - operator: Exists
      effect: NoSchedule
    - operator: Exists
      effect: NoExecute

  resources:
    requests:
      cpu: 100m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

## ── Kube State Metrics ───────────────────────────────────────
kubeStateMetrics:
  enabled: true

kube-state-metrics:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

## ── Component Scrapers ───────────────────────────────────────
## NOTE: EKS managed control plane - these are NOT accessible
kubeApiServer:
  enabled: true

kubelet:
  enabled: true
  serviceMonitor:
    cAdvisor: true

coreDns:
  enabled: true

## Disabled - EKS managed control plane, endpoints not exposed
kubeControllerManager:
  enabled: false

kubeScheduler:
  enabled: false

kubeEtcd:
  enabled: false

## Disabled - EKS uses VPC CNI
kubeProxy:
  enabled: false

## ── Default Alerting Rules ────────────────────────────────────
defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: false
    configReloaders: true
    general: true
    k8sContainerCpuUsageSecondsTotal: true
    k8sContainerMemoryCache: true
    k8sContainerMemoryRss: true
    k8sContainerMemorySwap: true
    k8sContainerResource: true
    k8sPodOwner: true
    kubeApiserverAvailability: true
    kubeApiserverBurnrate: true
    kubeApiserverHistogram: true
    kubeApiserverSlos: true
    kubeControllerManager: false
    kubelet: true
    kubeProxy: false
    kubePrometheusGeneral: true
    kubePrometheusNodeRecording: true
    kubernetesApps: true
    kubernetesResources: true
    kubernetesStorage: true
    kubernetesSystem: true
    kubeSchedulerAlerting: false
    kubeSchedulerRecording: false
    kubeStateMetrics: true
    network: true
    node: true
    nodeExporterAlerting: true
    nodeExporterRecording: true
    prometheus: true
    prometheusOperator: true
